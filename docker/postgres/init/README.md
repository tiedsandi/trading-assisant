# PostgreSQL initialization

Place optional development-only `.sql`, `.sql.gz`, or executable `.sh` initialization files here.
They run only when the PostgreSQL data volume is created for the first time.
